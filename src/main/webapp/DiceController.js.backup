/**
 * @author Isak Aasemoen Aardal
 */

import Dice from "./Dice.js"

// alert("loaded DiceController")
// document.getElementById("data-dicebutton").addEventListener("click", alert("Button pressed"));

export class DiceController {

  _root = "root";
  _inputElement = document.getElementById(_root).querySelector("data-dicebutton");
  _outputElement;

  constructor(root = document.getElementById("root")) {
    this._root = root
    this.run = this.run.bind(this);
    this.rollDice = this.rollDice.bind(this);
  }

  run() {
    alert("lmao");
    btRef = this._root.querySelector("data-dicebutton");
    btRef.addEventListener("click", this.rollDice);
    this.rollDice = this.rollDice.bind(this);
    diceoutput = this._root.querySelector("data-diceoutput");
    this.diceoutput.innerHtml = _outputElement;
  }

  rollDice() {
    Dice.roll();
    this._outputElement = Dice.getValue();
  }
}

// Generated by CoffeeScript 2.4.1
// var DiceController;

// import Dice from "./Dice.js";

// DiceController = (function() {
//   class DiceController {
//     constructor(root = document.getElementById("root")) {
//       this._root = root;
//       this.run = this.run.bind(this);
//       this.rollDice = this.rollDice.bind(this);
//     }

//     run() {
//       var btRef, diceOutput;
//       alert('Running');
//       btRef = this._root.querySelector("data-dicebutton");
//       btRef.addEventListener("click", this.rollDice);
//       diceOutput = this.root.querySelector("data-dicebutton");
//       return this.diceOutput.innerHtml = this._outputElement;
//     }

//     rollDice() {
//       alert('Rolling Dice');
//       Dice.roll;
//       return this._outputElement = Dice.getValue();
//     }

//   };

//   DiceController.prototype._root = document.getElementById("root");

//   DiceController.prototype._inputElement = document.getElementById(_root).querySelector("data-dicebutton");

//   DiceController.prototype._outputElement = "no value";

//   return DiceController;

// }).call(this);